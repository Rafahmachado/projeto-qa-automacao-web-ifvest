{
  "name": "teste-cypress",
  "version": "1.0.0",
  "scripts": {
    "test:run": "cypress run",
    "report:generate": "mochawesome-merge cypress/reports/*.json > mochawesome.json && mochawesome-report-generator mochawesome.json",
    "test:full": "npm run test:run && npm run report:generate"
  },
  "devDependencies": {
    "cypress": "^15.0.0",
    "cypress-axe": "^1.7.0",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^5.2.0"
  }
}
